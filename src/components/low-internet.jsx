import React from 'react';
import {API_BASE} from "../utils/consts";

const LowInternet = () => {
  return (
    <div>
      <img src={`${API_BASE}/bot/icons/shutterstock-icon.png`} alt=""/>
    </div>
  );
};

export default LowInternet;
